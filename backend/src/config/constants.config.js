const UserRoles = {
  ADMIN: "admin",
  CUSTOMER: "customer"
}

const StatusType = {
    ACTIVE: "active",
    INACTIVE: "inactive"
}
const ProductStatus = {
  AVAIL: "available",
  UNAVAIL: "unavailable"
}

const fileFilterType = {
  IMAGE:"image",
  DOC:"doc",
  VIDEO:"video"
}

module.exports = {
    UserRoles,
    StatusType,
    fileFilterType,
    ProductStatus
}