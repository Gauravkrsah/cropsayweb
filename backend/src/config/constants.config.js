const UserRoles = {
  ADMIN: "admin",
  CUSTOMER: "customer"
}

const StatusType = {
    ACTIVE: "active",
    INACTIVE: "inactive"
}

const fileFilterType = {
  IMAGE:"image",
  AUDIO:"audio",
  DOC:"doc",
  VIDEO:"video"
}

module.exports = {
    UserRoles,
    StatusType,
    fileFilterType
}